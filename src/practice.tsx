// 1. The app name never changes
const appName = "GOAT Notes";

// 2. This array stores notes and we push new ones later
const notes = [];

// 3. This greeting changes based on time of day
let greeting = "Good morning";
greeting = "Good afternoon";

// 4. This user object is created once, but their age may change
const user = { name: "Ruben", age: 19 };
user.age = 20;

// 5. This token changes every time the user logs in
let sessionToken = "abc123";
sessionToken = "def456";
